{
  "name": "nexus-chat",
  "version": "0.2.0",
  "private": true,
  "description": "Protótipo funcional de comunicação interna, suporte e gestão de recursos corporativos.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check public/app.js"
  },
  "dependencies": {
    "@fastify/static": "^10.1.3",
    "fastify": "^5.12.1",
    "socket.io": "^4.8.3"
  },
  "engines": {
    "node": "24.x"
  }
}
